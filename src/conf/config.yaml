# conf/config.yaml
defaults:
  - _self_

data:
  file: "/hpc2hdd/home/mgong081/Projects/DeepSynergy/data/data_test_fold0_tanh.p_1.gz"

model:
  layers: [8182, 4096, 1]
  dropout: 0.5
  input_dropout: 0.2
  learning_rate: 0.00001

training:
  batch_size: 64
  epochs: 4
  early_stopping_average_over: 2

# dir
hydra:
  run:
    dir: outputs/${now:%m-%d_%H-%M}
